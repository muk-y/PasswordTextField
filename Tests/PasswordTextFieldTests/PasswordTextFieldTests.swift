    import XCTest
    @testable import PasswordTextField

    final class PasswordTextFieldTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(PasswordTextField().text, "Hello, World!")
        }
    }
